# Copy to project root as .env (docker compose reads it automatically)
#   cp mac-cloud/.env.example .env

POSTGRES_DB=amlak
POSTGRES_USER=amlak
POSTGRES_PASSWORD=change-me-strong-password
POSTGRES_PORT=5432

AMLAK_API_PORT=8787
AMLAK_API_TOKEN=change-me-long-secret-token
CORS_ORIGIN=https://amlakrrgroup.netlify.app

# Push server for incoming calls when app is closed
FCM_PUSH_URL=http://fcm-push:3200
FCM_PUSH_PORT=3200
